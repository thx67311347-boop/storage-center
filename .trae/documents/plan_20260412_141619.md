## 任务目标
将文档图标的图标更改为位于路径`C:\Users\thx67\OneDrive\Pictures\Storage center\files.png`的图片文件。

## 实施步骤

### 1. 复制图片文件到项目目录
- 将用户指定的PNG文件复制到项目的`public`文件夹中，重命名为`document-icon.png`
- 这样可以通过相对路径访问图片文件，避免浏览器的安全限制

### 2. 修改Icon.tsx文件
- 在`Icon`组件中，为`name === 'document'`的情况添加特殊处理逻辑
- 参考现有的folder和image图标实现方式，使用`<img>`标签加载图片文件
- 确保文档图标在不同尺寸下都能正常显示，保持与原图标相同的尺寸比例和显示位置

### 3. 测试验证
- 运行开发服务器，检查文档图标是否正常显示
- 验证文档图标在不同尺寸下的显示效果
- 确保其他图标保持不变，仍然正常工作

### 4. 部署同步
- 提交更改到Git仓库
- 推送到远程仓库
- 部署到Vercel生产环境，确保更改同步到目标网站

## 技术要点
- 使用与folder和image图标相同的实现方式，确保一致性
- 保持图片的尺寸比例，使用`objectFit: 'contain'`
- 确保文档图标在点击和非点击状态下都能正常显示
- 验证部署后文档图标在生产环境中正常工作