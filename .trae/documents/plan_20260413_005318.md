## 实现 MEGA 会话复用机制

根据用户提供的详细方案，我将实施以下步骤：

### 1. 创建共享的会话管理模块
- 创建 `app/lib/mega-storage.ts` 文件
- 实现全局会话管理，包括会话有效性检查、自动重新登录等功能
- 添加 `keepalive` 设置保持连接活跃

### 2. 修改 MEGA API 路由
- 修改 `app/api/mega/upload/route.ts` 使用新的会话管理
- 修改 `app/api/mega/session/route.ts` 使用新的会话管理
- 修改 `app/api/mega/download/route.ts` 使用新的会话管理
- 修改 `app/api/mega/delete/route.ts` 使用新的会话管理

### 3. 测试实现效果
- 测试多次上传请求是否复用同一个会话
- 测试会话失效时是否能自动重新登录
- 验证上传功能是否正常工作

### 4. 优化和调整
- 根据测试结果进行必要的调整
- 确保代码符合生产环境要求

## 预期效果

- ✅ 会话复用（避免频繁登录）
- ✅ 自动检测会话有效性
- ✅ 失效后自动重新登录
- ✅ 支持 `sid` 持久化（可选）
- ✅ 解决因 IP 变动导致 MEGA 要求修改密码的问题